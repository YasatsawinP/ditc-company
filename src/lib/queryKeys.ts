export const queryKeys = {
  // Example pattern — expand per feature:
  // users: {
  //   all: ['users'] as const,
  //   list: () => [...queryKeys.users.all, 'list'] as const,
  //   detail: (id: string) => [...queryKeys.users.all, 'detail', id] as const,
  // },
}
